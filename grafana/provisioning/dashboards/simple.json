{
  "annotations": { "list": [] },
  "editable": true,
  "panels": [
    {
      "type": "timeseries",
      "title": "Requests per Service",
      "targets": [
        {
          "expr": "sum by (service) (increase(http_requests_total[1m]))",
          "legendFormat": "{{service}}"
        }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 24, "h": 8 }
    },
    {
      "type": "timeseries",
      "title": "Total Traffic",
      "targets": [
        {
          "expr": "sum(increase(http_requests_total[1m]))",
          "legendFormat": "total"
        }
      ],
      "gridPos": { "x": 0, "y": 8, "w": 12, "h": 8 }
    },
    {
      "type": "timeseries",
      "title": "Top Services",
      "targets": [
        {
          "expr": "topk(5, sum by (service) (increase(http_requests_total[1m])))",
          "legendFormat": "{{service}}"
        }
      ],
      "gridPos": { "x": 12, "y": 8, "w": 12, "h": 8 }
    }
  ],
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["microservices"],
  "title": "Microservices Overview",
  "version": 2
}